#include <syscall.h>
#include <stdio.h>
//#include "userprog/process.c"
int
main (void)
{
 	int i,j;
 	char *c;
 	c="b";
 	j=process_execute(c);
 	for(i=0;i<11;i++)
 	{
 		for(j=0;j<100000;j++);
 		if(i==10)
 		{
 			i=0;
 		}
 		printf("%d  ",i);
 	}
}
